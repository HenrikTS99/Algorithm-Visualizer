import java.awt.*;

public class NumColumn {
    Color color;

    public NumColumn(int num) {
        setColor(num);
    }

    private void setColor(int num) {
        // TODO: set color depending on height of column
    }
}
